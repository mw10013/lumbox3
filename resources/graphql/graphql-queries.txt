query Users {
  users { id email locked_at created_at groups }
}

query User {
  user(id: "1") { id email created_at }
}

mutation RegisterUser($register_user_input: RegisterUserInput!) {
  register_user(input: $register_user_input) {
  	user {
      id email
    }
  }
}

mutation Login($login_input: LoginInput!) {
 login(input: $login_input) {
  	user { id email }
 }
}

mutation Logout {
  logout {
    user { id email }
  }
}

# Variables

{
  "register_user_input": {
    "email": "bee@sting.com",
    "password": "letmein"
  },
  "delete_user_by_email_input": "bee@sting.com",
  "login_input": {
    "email": "bee@sting.com",
    "password": "letmein"
  }
}
